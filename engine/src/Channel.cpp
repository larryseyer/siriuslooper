#include "sirius/Channel.h"

// Session-2 placeholder TU. Channel is currently a plain aggregate with
// no out-of-line behaviour; this file exists so M3 can land non-trivial
// constructors / member functions without re-touching CMakeLists.
